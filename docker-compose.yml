services:
  mongodb:
    image: mongo:latest
    container_name: franchise-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: franchise_db
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: